syntax = "proto3";

message Requirement {
    string content = 1;
    string speaker = 2;
    string token = 3;
}

message Audio {
    bytes data = 1;
    string error = 2;
}

service AudioGenerationService {
    rpc GenerateAudio(Requirement) returns (Audio);
}
