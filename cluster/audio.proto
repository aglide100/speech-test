syntax = "proto3";

message Requirement {
    string content = 1;
    string speaker = 2;
}

message Audio {
    bytes data = 1;
}

service AudioGenerationService {
    rpc GenerateAudio(Requirement) returns (Audio);
}
